# Copy to keystore.properties (NOT committed — see .gitignore) and fill in.
# Create the upload key once:
#   keytool -genkeypair -v -keystore neonhorde-upload.jks -alias upload -keyalg RSA -keysize 4096 -validity 10000
storeFile=../neonhorde-upload.jks
storePassword=
keyAlias=upload
keyPassword=
